---
title: "UnivariateAssignment"
author: "Julie Raiguel"
date: "January 30, 2018"
output: html_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

```{r}
trees = read.csv('../data/treedata_subset.csv')

head(trees)

```

```{r}
acerrub_subset = subset(trees, subset = spcode == 'ACERRUB')

head(arubrum_subset)

abiefra_subset= subset(trees, subset = spcode == 'ABIEFRA')
abiefra_subset$spcode = droplevels(abiefra_subset$spcode)

head(abiefra_subset)

pairs()

```
```{r}

```



